import blockContent from './blockContent'
import countries from './countries'
import users from './users'
import places from "./places";

export const schemaTypes = [places, users, countries, blockContent]
